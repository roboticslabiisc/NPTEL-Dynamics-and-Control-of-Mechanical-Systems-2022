clc % Clears the command window
clear % Clears the workspace

a = 3;
b = pi; % Constant pi
c = 2;
d = a + b -c; % Addition and subtraction
m = a*b; % Multiplication
n = a/c; % Division

p = a^3; % raised to the power
e = exp(a); % exponential function

s1 = sin(pi/3); % sine function with inputs in radian
s2 = sind(60); % sine function with inputs in degrees
t1 = tand(45); % tangent function with inputs in degrees

ac1 = acos(0); % inverse cosine function with output in radians
ac2 = acosd(0); % inverse cosine function with output in degrees
at1 = atand(1); % basic inverse tangent function
at2 = atan2d(-1,-1); % invese tangent function with conisideration of quadrant

L1 = log(a); % Natural logarithmic function
L2 = log10(a); % Base 10 logarithm

